import { FundoRoxoDirective } from './fundo-roxo.directive';

describe('FundoRoxoDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoRoxoDirective();
    expect(directive).toBeTruthy();
  });
});
