import { FundoMaisRoxoDirective } from './fundo-mais-roxo.directive';

describe('FundoMaisRoxoDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoMaisRoxoDirective();
    expect(directive).toBeTruthy();
  });
});
